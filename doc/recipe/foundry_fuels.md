# Foundry Fuels

### Required fields

- `"fluid"` Fluid used as fuel
- `"speed"` Define the speed boost for the foundry
- `"usage"` Define the fuel usage

```js
{
  "type": "synergy:foundry_fuels",
  "fluid": <fluid-stack>,
  "speed": <float>,
  "usage": <float>
}
```
